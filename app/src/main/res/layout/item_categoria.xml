<?xml version="1.0" encoding="utf-8"?>
<!--
    Grelha 3×2: o GridLayoutManager com spanCount=3 VERTICAL trata das 3 colunas.
    match_parent na largura + altura fixa garante células uniformes.
    background usa drawable com cantos redondos — bg_card_normal por defeito,
    substituído por bg_card_ativo no Adapters.kt quando selecionado.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/itemCategoriaRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="10dp"
    android:background="@drawable/bg_card_normal">

    <!-- Ícone com fundo próprio arredondado -->
    <FrameLayout
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:background="@color/cat_icone_bg">

        <ImageView
            android:id="@+id/imgCategoria"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:layout_gravity="center"
            android:src="@android:drawable/ic_menu_agenda"
            android:contentDescription="Ícone" />
    </FrameLayout>

    <View android:layout_width="1dp" android:layout_height="6dp" />

    <TextView
        android:id="@+id/tvNomeCategoria"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/texto_inativo"
        android:textSize="11sp"
        android:gravity="center"
        android:maxLines="2"
        android:ellipsize="end"
        android:lineSpacingMultiplier="1.15" />

</LinearLayout>